<Widget>
    canvas.before:
        Color:
            rgba: 1, 1, 0, 1
            #TODO How does this work? and fix when known
    color: 0, 0, 1, 1

BoxLayout:
    orientation: 'vertical'
    BoxLayout:
        TextInput:
            text: '1'
            id: text_input
            multiline: False
            on_text_validate: app.handle_conversion(text_input.text)
        BoxLayout:
            orientation: 'vertical'
            Button:
                text: 'Up'
                on_press: app.handle_increment(1)
            Button:
                text: 'Down'
                on_press: app.handle_increment(-1)
    Button:
        text: 'Convert to KM'
        on_press: app.handle_conversion(text_input.text)
    Label:
        text: ''
        id: label_output